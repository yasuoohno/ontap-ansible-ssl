#
# Administrator ID and SVM for ansible ONTAP modules.
#
ANSIBLE_USER=admin_or_vsadmin
ANSIBLE_SVM=svmname_used_in_ansible

#
# Cluster administrator ID and cluster admin SVM name and ip address to configure ONTAP
#
ONTAP_CLUSTER=cluster_admin_svm
ONTAP_ADMIN=admin
ONTAP_CLUSTER_MGMT_IP=cluster_management_ip_address

#
# filename for public and private key pair
#
PUBLICKEY_FILE=$ANSIBLE_SVM-$ANSIBLE_USER.pem
PRIVATEKEY_FILE=$ANSIBLE_SVM-$ANSIBLE_USER.key
